// Centralized base URL for API and media assets.
// For client-side code (Next.js), set NEXT_PUBLIC_BASE_URL in your environment.
export const BASE_URL: string = process.env.NEXT_PUBLIC_BASE_URL || 'http://localhost:4004';
